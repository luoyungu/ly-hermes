# 贡献指南

感谢你愿意参与 LyHermes。这个项目还在快速演进，贡献时请尽量保持改动聚焦、可验证、容易回滚。

## 开发流程

1. Fork 仓库并创建功能分支。
2. 运行 `npm install` 安装依赖。
3. 使用 `npm run dev` 启动开发环境。
4. 提交前运行 `npm run typecheck`。
5. 如果改动影响 UI、IPC、远程 API 或数据结构，请在 PR 中说明测试方式和兼容性影响。

## 提交建议

- 一个 PR 尽量只解决一个问题或实现一个功能。
- 不提交构建产物、截图、日志、本地数据库、`.env` 或真实 token。
- 不把用户数据、聊天记录、API Key、远程连接 token 或签名证书放进示例和测试夹具。
- 修改公开 API、配置文件格式或本地数据迁移时，请同步更新 README、CHANGELOG 或相关 docs。

## 代码风格

- TypeScript 优先保持现有模块边界和命名风格。
- UI 改动应兼顾桌面端和 Web 端入口。
- 新增依赖前请确认必要性，并说明它解决的问题。

## 本地检查

```bash
npm run typecheck
npm run lint
```

如果某个检查暂时无法运行，请在 PR 中写明原因和你已经完成的人工验证。
